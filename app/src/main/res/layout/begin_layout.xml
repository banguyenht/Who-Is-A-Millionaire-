<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_online_blank">

    <ImageView
        android:id="@+id/iconALTP"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:src="@drawable/atp_icon_retry" />

    <ImageView
        android:id="@+id/circle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iconALTP"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-90dp" />

    <Button
        android:id="@+id/btn_play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="182dp"
        android:layout_marginLeft="10dp"
        android:background="@drawable/bg_play"
        android:text="Now Play"
        android:textSize="19sp" />

    <Button
        android:id="@+id/btn_hight_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignTop="@+id/btn_play"

        android:layout_marginTop="83dp"
        android:background="@drawable/bg_hightscore"
        android:text="Hightscore"
        android:textSize="19sp" />
</RelativeLayout>